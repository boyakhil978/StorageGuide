# Todo for storageguide

Add a menu that to configure common settings (acessible by the Mod Menu mod).

Add color settings for the higlight border that can be configured in the mod menu. Add a picker if possible.

Also the hotbar border around the selected item should also change color based on if the item is found our not.


Add operater menu for easy gui management of settings

Add operator settings to enable disable slopiness detector, enable force clients to use mod. Also allow the operator to modify the slopiness cooldown.

Add an edit chest menu option to mark that chest to be excuded from the slopiness detection.

Add a slopiness detection menu to view slopiness instances sorted per player (this menu should show all slopiness instances not effected by the cooldown). This menu should be available to everyone. 

Modify the whole mod to allow creating multiple rectangular grids in one server (need to separate the keybind for editing and creating configurations). Before this implement a confirmation process for adding new configurations to improve the onboarding process.